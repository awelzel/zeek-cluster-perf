# Zeek Cluster Benchmarking

## Quickstart

Starting up a Zeek cluster as it is configured via cluster-config.yaml:

    $ ln -s cluster-config-local.yaml cluster-config.yaml
    $ zeek supervisor.js


Use ``run.sh`` for starting cluster runs using cluster-config.yaml.
